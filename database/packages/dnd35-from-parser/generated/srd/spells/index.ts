// Auto-generated by the SRD pipeline — do not edit manually
import type { PowerSeed } from "@/database/packages/dnd35/v1/spells/types.ts";

import { CANTRIPS } from "./cantrips.ts";
import { LEVEL_1_SPELLS } from "./level1.ts";
import { LEVEL_2_SPELLS } from "./level2.ts";
import { LEVEL_3_SPELLS } from "./level3.ts";
import { LEVEL_4_SPELLS } from "./level4.ts";
import { LEVEL_5_SPELLS } from "./level5.ts";
import { LEVEL_6_SPELLS } from "./level6.ts";
import { LEVEL_7_SPELLS } from "./level7.ts";
import { LEVEL_8_SPELLS } from "./level8.ts";
import { LEVEL_9_SPELLS } from "./level9.ts";

export {
  CANTRIPS,
  LEVEL_1_SPELLS,
  LEVEL_2_SPELLS,
  LEVEL_3_SPELLS,
  LEVEL_4_SPELLS,
  LEVEL_5_SPELLS,
  LEVEL_6_SPELLS,
  LEVEL_7_SPELLS,
  LEVEL_8_SPELLS,
  LEVEL_9_SPELLS,
};

export type PowerSeedWithLevel = PowerSeed & { level: number };

export const ALL_SPELLS: PowerSeedWithLevel[] = [
  ...CANTRIPS.map((p) => ({ ...p, level: 0 })),
  ...LEVEL_1_SPELLS.map((p) => ({ ...p, level: 1 })),
  ...LEVEL_2_SPELLS.map((p) => ({ ...p, level: 2 })),
  ...LEVEL_3_SPELLS.map((p) => ({ ...p, level: 3 })),
  ...LEVEL_4_SPELLS.map((p) => ({ ...p, level: 4 })),
  ...LEVEL_5_SPELLS.map((p) => ({ ...p, level: 5 })),
  ...LEVEL_6_SPELLS.map((p) => ({ ...p, level: 6 })),
  ...LEVEL_7_SPELLS.map((p) => ({ ...p, level: 7 })),
  ...LEVEL_8_SPELLS.map((p) => ({ ...p, level: 8 })),
  ...LEVEL_9_SPELLS.map((p) => ({ ...p, level: 9 })),
];
