// BaseRules remains re-exported here so generic consumers (character sheet
// factories, sections) don't need to know the wizard hooks moved to dnd3.5/.
export { type BaseRules } from "../dnd3.5/levelUp/useLevelWizard.ts";
